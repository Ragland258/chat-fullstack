2026-08-01 找回密码界面改造

1. 关闭按钮改为轻量灰色样式，取消红色圆底。
2. 增加头像与账号输入框之间的距离。
3. 新增 ForgotPasswordPage 并接入忘记密码入口。
4. 验证码请求复用 POST /get_verify 接口，带 60 秒倒计时。
5. 密码重置客户端请求预留为 POST /reset_password。
6. GateServer 目前尚未注册 /reset_password 路由。

验证结果：Qt 6.10.1 MinGW Debug 构建和 chat.exe 启动测试通过。
